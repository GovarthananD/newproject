var name = ('ganesh, suresh, vignesh, premkumar, sakthi, saran, joseph, naveen');
function caps (name) {
    return name.toLowerCase().splice('').map(function (word) {
        return (word.charAt(0).toUpperCase() + word.slice(1));
    }).join('');
}
console.log(caps(name));